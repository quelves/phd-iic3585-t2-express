extends layout

block content
    .container
        form.form-signin
            h2.form-signin-heading Por favor iniciar
            label.sr-only(for='inputEmail') Email
            input#inputEmail.form-control(type='email', placeholder='Email', required='', autofocus='')
            label.sr-only(for='inputPassword') Clave
            input#inputPassword.form-control(type='password', placeholder='Clave', required='')
            .checkbox
                label
                    input(type='checkbox', value='remember-me')
                    |  Acordarme
            button.btn.btn-lg.btn-primary.btn-block(type='submit') iniciar